//created function for button, directing to projects but later down the road will direct possible to something else

const button = document.querySelector('button');

button.addEventListener('click', function() {
    window,location.href = '#projects';
console.log('Clicked')
})

